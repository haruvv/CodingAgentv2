name: CI

on:
  pull_request:
    branches: [main]

jobs:
  placeholder-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Placeholder CI
        run: echo "CI passed (placeholder)"
